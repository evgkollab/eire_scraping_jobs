# Core scraping & automation
selenium==4.39.0

# Data wrangling
pandas==2.3.3
pyarrow==22.0.0        # needed so BigQuery loads DataFrames without fallback to pandas-gbq

# Google Cloud SDK – BigQuery client and auth
google-cloud-bigquery==3.38

# Environment-variable convenience
python-dotenv==1.2.1
db-dtypes
thefuzz
