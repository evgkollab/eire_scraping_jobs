# Core scraping & automation
selenium==4.20.0

# Data wrangling
pandas==2.2.2
pyarrow==16.0.0        # needed so BigQuery loads DataFrames without fallback to pandas-gbq

# Google Cloud SDK – BigQuery client and auth
google-cloud-bigquery==3.17.2

# Environment-variable convenience
python-dotenv==1.0.1
db-dtypes
thefuzz
